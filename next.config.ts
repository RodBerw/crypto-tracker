import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["coin-images.coingecko.com"], // Adicione esse domínio à lista
  },
};

export default nextConfig;
